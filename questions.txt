questions:
    1. difference between promt engineering and context engineering
    2. when to create a spec file
    3. what is deep agent and how claude fits into this category
    4. check why /lsp is not working


pending:
    1. list all slash commands
        - /init 
        - /mcp
        - /security-review
        - /model
        - /exit
        - /clear
        - /compact
        - /agents
        - /config
        - /cost
        - /ide
        - /hooks
        - /memory
        - /rewind
        - /context

    2. Add all hooks to hooks directory.
    3. check cursor equivalent of /rewind
    4. Claude code plugin (/plugin) - read from outside, not from Eden Marco tutorial
        
command:
    - claude --help
    - 

Note:
 - claude memory is very similar to cursor rules
 - In custom shell commands we can run shell or bash scripts